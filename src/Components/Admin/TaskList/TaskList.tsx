import "./TaskList.css";

function TaskList(): JSX.Element {
    return (
        <div className="TaskList">
			This is screen which which only admin can access
        </div>
    );
}

export default TaskList;
